// Side-effect module that registers every sport adapter. Populated as adapters
// are added in tasks 9-20. Importing this file is what causes the registry to
// see the adapters — keep this as the single import point in scripts/etc.
export {};
